{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        },

        { //chrome debagger設定
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "file": "${file}"
        }
    ]
 }